spring:
  application:
    name: whatsappai
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

server:
  port: ${PORT:8080}

openai:
  api-key: ${OPENAI_API_KEY}

config:
  lock:
    timer: ${LOCK_SECONDS}
  finalize:
    timer: ${FINALIZE_SECONDS}

logging:
  level:
    root: INFO