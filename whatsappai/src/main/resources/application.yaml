spring:
  application:
    name: whatsappai
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

server:
  port: ${PORT:8080}

openai:
  api-key: ${OPENAI_API_KEY}

config:
  lock:
    timer: ${LOCK_SECONDS:20}
  finalize:
    timer: ${FINALIZE_SECONDS:30}

logging:
  level:
    root: INFO